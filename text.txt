Anu love 

<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Happy Valentine’s Day Anu 💖</title>

<style>
body {
    margin: 0;
    height: 100vh;
    background: linear-gradient(135deg, #ff4b6e, #ffb3c6);
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: 'Segoe UI', sans-serif;
    overflow: hidden;
}

.card {
    background: rgba(255,255,255,0.96);
    border-radius: 28px;
    padding: 30px;
    text-align: center;
    box-shadow: 0 25px 60px rgba(0,0,0,0.35);
    max-width: 440px;
    animation: fadeIn 1.3s ease;
}

h1 {
    color: #ff2f68;
}

.slider {
    width: 230px;
    height: 230px;
    border-radius: 50%;
    overflow: hidden;
    margin: 20px auto;
    border: 7px solid #ff4b6e;
    box-shadow: 0 12px 30px rgba(255,75,110,0.6);
}

.slider img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    animation: fade 6s infinite;
}

@keyframes fade {
    0% {opacity:1}
    30% {opacity:1}
    33% {opacity:0}
    35% {opacity:1}
}

p {
    font-size: 18px;
    color: #444;
    line-height: 1.6;
}

button {
    margin-top: 18px;
    padding: 13px 32px;
    border: none;
    border-radius: 30px;
    background: linear-gradient(135deg,#ff2f68,#ff7aa2);
    color: white;
    font-size: 16px;
    cursor: pointer;
    box-shadow: 0 10px 25px rgba(0,0,0,0.25);
    transition: 0.3s;
}

button:hover { transform: scale(1.08); }

.surprise {
    display: none;
    margin-top: 18px;
    font-size: 19px;
    color: #ff2f68;
    line-height: 1.6;
    min-height: 120px;
}

.heart {
    position: fixed;
    bottom: -30px;
    font-size: 24px;
    animation: floatUp 7s linear infinite;
}

@keyframes floatUp {
    from { transform: translateY(0) scale(1); opacity: 0.9; }
    to { transform: translateY(-120vh) scale(1.6); opacity: 0; }
}

@keyframes fadeIn {
    from { opacity: 0; transform: scale(0.7); }
    to { opacity: 1; transform: scale(1); }
}
</style>
</head>

<body>

<!-- 🎶 Background music -->
<audio autoplay loop>
  <source src="love.mp3" type="audio/mpeg">
</audio>

<div class="card">
    <h1>Happy Valentine’s Day, Anu 💘</h1>

    <!-- 💞 Slideshow -->
    <div class="slider">
        <img id="slide" src="anu1.jpg">
    </div>

    <p>Anu, this little surprise is just a small reflection of what you mean to me…</p>

    <button onclick="openGift()">Open Your Surprise 💝</button>

    <!-- 💌 Typewriter love letter -->
    <div class="surprise" id="surprise"></div>

    <p style="margin-top:15px;color:#888;font-size:14px;">
        Forever yours ❤️
    </p>
</div>

<script>
/* 💞 Slideshow */
let images = ["anu1.jpg", "anu2.jpg", "anu3.jpg"];
let index = 0;
setInterval(() => {
    index = (index + 1) % images.length;
    document.getElementById("slide").src = images[index];
}, 3000);

/* 💌 Typewriter effect */
const message = "💖 Anu, every heartbeat of mine whispers your name. You are my favorite feeling, my safest place, and my most beautiful miracle. Loving you is the easiest and most magical thing I have ever done. Happy Valentine’s Day, my love. 💖";

let i = 0;
function typeWriter() {
    if (i < message.length) {
        document.getElementById("surprise").innerHTML += message.charAt(i);
        i++;
        setTimeout(typeWriter, 40);
    }
}

function openGift() {
    document.getElementById("surprise").style.display = "block";
    typeWriter();
}

/* ❤️ Floating hearts */
function createHeart() {
    const heart = document.createElement("div");
    heart.className = "heart";
    heart.innerHTML = "❤️";
    heart.style.left = Math.random() * 100 + "vw";
    heart.style.animationDuration = (4 + Math.random() * 5) + "s";
    document.body.appendChild(heart);
    setTimeout(() => heart.remove(), 9000);
}
setInterval(createHeart, 300);
</script>

</body>
</html>